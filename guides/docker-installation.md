# Docker Installation

## Install Docker

```bash
sudo apt update
sudo apt install docker.io -y
```

## Enable Docker

```bash
sudo systemctl enable docker
sudo systemctl start docker
```

## Verify Installation

```bash
docker --version
```
